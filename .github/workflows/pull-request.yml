name: Pull request

on:
  pull_request:
    types: [ labeled ]

jobs:
  dev:
    runs-on: ubuntu-latest
    if: ${{ github.event.label.name == 'deploy-dev' }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4


